#!/bin/bash

if [ -d "$DOTFILES_PATH/scripts" ]; then
  for func_file in "$DOTFILES_PATH"/scripts/*; do
    if [ -f "$func_file" ]; then
      source "$func_file"
    fi
  done
  unset func_file # Clean up loop variable
fi